Todo List

1. Add CSS variables: Add color variables to styles.css (light & dark tokens).
2. Create dark-theme class: Add a .dark-theme rule that overrides the variables.
3. Add toggle markup: Insert a toggle button (accessible) into index.html header.
4. Add toggle JS logic: Add toggle code to scripts.js to add/remove .dark-theme.
5. Persist preference: Save/read choice from localStorage on load and toggle.
6. Add accessibility support: Ensure ARIA labels, keyboard focus, and contrast checks.
7. Include toggle on other pages: Add the same markup/JS to about.html, services.html, contact.html.
8. Test & adjust colors: Manually test pages and tweak variable colors for contrast.
9. Support prefers-color-scheme: Add CSS fallback that respects prefers-color-scheme.
10. Commit changes + docs: Commit edits and add a short README note about the feature.
